from setuptools import setup

setup(name='batcore',
      version='0.1',
      description="batcore",
      long_description="",
      author='TODO',
      author_email='todo@example.org',
      license='TODO',
      packages=['batcore'],
      zip_safe=False,
      install_requires=['numpy', 'scipy', 'tqdm'],
      )
